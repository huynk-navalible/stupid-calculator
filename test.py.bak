#!/run/current-system/sw/bin/env python3

# the second part
def calculate(bitch, wth):
    if bitch == "+":
        result = sum(numbers)
    elif bitch == "-":
        result = numbers[0]
        for num in numbers[1:]:
            result -= num
    elif bitch == "*":
        result = 1
        for num in numbers:
            result *= num
    elif bitch == "/" or bitch == ":":
        result = numbers[0]
        for num in numbers:
            if num == 0:
                return "Hey, do u forgot basic math or smth"
            result /= num
    else:
        return "i think you didnt hear my yapping [im crying huhu]"


    return round(result, 2)
    

# the first part
bitch = input("choose pls (+,-,* or / )\n")
wth = input("enter the number (seperate it by spaces vro):\n").split()

try:
    numbers = [float(w) for w in wth]
    output = calculate(bitch, wth)
    print("The Result for the thing u want to calculate is ", output)
except ValueError:
    print("What did u just typed in ???")
